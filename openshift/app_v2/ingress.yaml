apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: app-for-test-connect-ingress

spec:
  rules:
    - host: route-ci00058876.apps.sh1.dev-gen.ca.sbrf.ru
      http:
        paths:
          - path: /getDual
            backend:
              serviceName:  connect-http-a
              servicePort: 8080